CREATE TABLE [dbo].[tam_post_plan_types] (
    [id]             TINYINT       NOT NULL,
    [plan_type_name] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_tam_post_plan_types] PRIMARY KEY CLUSTERED ([id] ASC)
);

